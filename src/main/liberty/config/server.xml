<?xml version="1.0" encoding="UTF-8"?><!--Generated by IBM TransformationAdvisor 
Thu May 28 15:13:25 UTC 2020--><server description="Configuration generated by binaryAppScanner">
    <featureManager>
        <!--The following features are available in all editions of Liberty.-->
        <feature>appSecurity-2.0</feature>
        <feature>ejbLite-3.2</feature>
        <feature>jdbc-4.1</feature>
        <feature>jndi-1.0</feature>
        <feature>jsp-2.3</feature>
        <feature>servlet-3.1</feature>
        <!--The following features are available in all editions of Liberty, except for Liberty Core.-->
        <feature>ejbRemote-3.2</feature>
    <feature>mpHealth-1.0</feature>
    </featureManager>
    <!-- This configuration was migrated on 2020-05-26 at 09:50:41 from the following location: /opt/IBM/WebSphere/AppServer70/profiles/AppSrv01 -->
    <!-- The binary scanner does not support the migration of all WebSphere traditional configuration elements. Check the binary scanner documentation for the list of supported configuration elements. -->
    <!-- The following WebSphere traditional variables are used by the configuration but were not migrated: [CELL, APP_INSTALL_ROOT, WAS_INSTALL_ROOT]. Remove these variables from the configuration or provide each one with an appropriate definition. -->
    <applicationManager autoExpand="true"/>
    <httpEndpoint host="*" httpPort="9080" httpsPort="9443" id="defaultHttpEndpoint"/>
    <enterpriseApplication location="DefaultApplication.ear"/>
    <jdbcDriver id="Derby_JDBC_Provider">
        <library>
            <file name="${DERBY_JDBC_DRIVER_PATH}/derby.jar"/>
        </library>
    </jdbcDriver>
    <dataSource id="Default Datasource" jdbcDriverRef="Derby_JDBC_Provider" jndiName="DefaultDatasource">
        <properties.derby.embedded connectionAttributes="upgrade=true" databaseName="${APP_INSTALL_ROOT}/${CELL}/DefaultApplication.ear/DefaultDB"/>
        <connectionManager agedTimeout="-1" connectionTimeout="180" maxIdleTime="1800" maxPoolSize="10" minPoolSize="1" purgePolicy="EntirePool" reapTime="180"/>
    </dataSource>
    <variable name="DERBY_JDBC_DRIVER_PATH" value="${WAS_INSTALL_ROOT}/derby/lib"/>
<application id="DefaultApplication" location="defaultapplication.war" name="DefaultApplication" type="war"/>
</server>